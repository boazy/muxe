# Muxe core reference

> Generated by `cargo run --locked -p muxe-core --bin generate_reference -- REFERENCE.md reference/host-support.tsv reference/action-descriptions.yaml`. Do not edit this file by hand.

The key capabilities, action identifiers, and execution defaults are generated from `muxe-core`. The host support mappings are generated from checked-in adapter evidence in `reference/host-support.tsv`. Action explanations and parameter schemas are loaded from `reference/action-descriptions.yaml`.

## Scope and evidence

Portable actions are host-independent candidates. An active adapter validates a candidate before configuration activation, and an adapter may reject an action that its host cannot represent.
Context references are type-checked while compiling configuration. Muxe resolves them against the attach-time origin and validates the concrete value again before dispatch. A missing context value prevents dispatch.

- **E1 — owned-fixture smoke:** An approved fresh-TempDir Herdr child completed `ping`, observed `Herdr` at its exact configured socket with protocol 20/version 0.8.2, then terminated gracefully and reaped its retained child. It did not smoke a portable action.
- **E2 — Herdr source evidence:** Adapter mapping and schema tests passed. The mappings below are source-tested, not live action tests.
- **E3 — Zellij qualified source evidence:** Pinned Rust 1.97.1 source tests passed (55 adapter, 10 protocol, 18 bridge lifecycle) plus the pinned WASM component build. This proves the listed source paths, not a live Zellij host; external artifact digest/grant verification remains blocked.

## Action schema

Use either a YAML mapping with `type` or a compact action string. In compact form, an unquoted `$origin…` token is a typed context reference; quote it to make it literal. A mapping context reference uses `{ $context: origin.… }`.

```yaml
action:
  type: command:execute
  program: cargo
  cwd:
    $context: origin.pane.cwd

# Compact form
action: tab:create workspace-id=$origin.workspace.id
```

### Action inventory

| Category | Actions |
|---|---|
| Menu | [`menu:open`](#menuopen), [`menu:return`](#menureturn), [`menu:quit`](#menuquit), [`menu.page:prev`](#menupageprev), [`menu.page:next`](#menupagenext) |
| Configuration | [`config:reload`](#configreload) |
| Keyboard | [`keyboard:send`](#keyboardsend) |
| Command | [`command:execute`](#commandexecute) |
| Tab | [`tab:create`](#tabcreate), [`tab:close`](#tabclose), [`tab:rename`](#tabrename), [`tab:focus`](#tabfocus), [`tab:move`](#tabmove), [`tab:swap`](#tabswap) |
| Pane | [`pane:create`](#panecreate), [`pane:split`](#panesplit), [`pane:close`](#paneclose), [`pane:focus`](#panefocus), [`pane:move`](#panemove), [`pane:swap`](#paneswap), [`pane:resize`](#paneresize), [`pane:zoom`](#panezoom), [`pane:fullscreen`](#panefullscreen), [`pane:floating`](#panefloating), [`pane:frame`](#paneframe) |
| Session | [`session:create`](#sessioncreate), [`session:attach`](#sessionattach), [`session:switch`](#sessionswitch), [`session:rename`](#sessionrename), [`session:detach`](#sessiondetach), [`session:quit`](#sessionquit), [`session:kill`](#sessionkill) |

## Portable action catalog

This catalog documents every portable action, its parameter schema, default execution policy, and host-specific adapter behavior.

### `menu:open`

Opens another menu by ID or renders an inline submenu.

- **Default mode:** synchronous broker execution
- **Capabilities:** none
- **Positional field (compact):** `menu`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `menu` | string (literal only) | no | — | ID of a top-level menu declared in configuration. Context references are not supported. Required unless submenu is used. |
| `submenu` | mapping | no | — | Inline submenu definition mapping. Required unless menu is used. |


> [!NOTE]
> **Zellij:** Muxe menu state; no portable Zellij Action. Separately, `muxe menu/pane open --host zellij` is source-proven to use the origin-client typed bridge for `NewTiledPane` (all four directions) or `NewFloatingPane` overlay with an exact argv/CWD vector and forced menu focus; no transient tab or live-host proof
>
> **Herdr:** Broker UI control; no Herdr RPC

### `menu:return`

Navigates back to the parent menu in the navigation stack, or closes the menu if at the root.

- **Default mode:** synchronous broker execution
- **Capabilities:** none

#### Parameters

None.


> [!NOTE]
> **Zellij:** Muxe menu state; no Zellij Action or shim equivalent
>
> **Herdr:** Broker UI control; no Herdr RPC

### `menu:quit`

Closes the active menu and returns keyboard focus to the captured origin pane.

- **Default mode:** synchronous broker execution
- **Capabilities:** none

#### Parameters

None.


> [!NOTE]
> **Zellij:** Muxe menu state; no Zellij Action or shim equivalent
>
> **Herdr:** Broker UI control; no Herdr RPC

### `menu.page:prev`

Navigates to the previous page of items in a paginated menu.

- **Default mode:** synchronous broker execution
- **Capabilities:** none

#### Parameters

None.


> [!NOTE]
> **Zellij:** Muxe menu state; no Zellij Action or shim equivalent
>
> **Herdr:** Broker UI control; no Herdr RPC

### `menu.page:next`

Navigates to the next page of items in a paginated menu.

- **Default mode:** synchronous broker execution
- **Capabilities:** none

#### Parameters

None.


> [!NOTE]
> **Zellij:** Muxe menu state; no Zellij Action or shim equivalent
>
> **Herdr:** Broker UI control; no Herdr RPC

### `config:reload`

Reloads Muxe configuration from disk and recompiles all menus.

- **Default mode:** synchronous broker execution
- **Capabilities:** none

#### Parameters

None.


> [!NOTE]
> **Zellij:** Broker configuration control; no Zellij RPC
>
> **Herdr:** Broker configuration control; no Herdr RPC

### `keyboard:send`

Sends simulated key events or text directly to the captured origin pane.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `keys` | sequence of keys or context | no | — | List of canonical key strings to send in sequence (for example ["ctrl+c"]). Exactly one of keys or text is required. |
| `text` | string or context | no | — | Plain text string to write directly to the pane input stream. Exactly one of keys or text is required. |


> [!NOTE]
> **Zellij:** Origin-targeted `WriteCharsToPaneId`/`WriteToPaneId`; finite bridge encoding supports text/C0/ESC-meta/VT100/CSI modifiers and rejects super/hyper/keypad/media/F13+ precisely
>
> **Herdr:** `pane.send_keys` or `pane.send_text` to the captured origin pane; keys/text must be strings

### `command:execute`

Spawns an external command as a supervised background process managed by the broker.

- **Default mode:** `detach`
- **Capabilities:** awaitable, detachable, cancellable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `program` | string or context | yes | — | Executable name or path to run. |
| `args` | sequence of strings or context | no | [] | Command-line arguments passed to the executable. |
| `cwd` | path or context | no | origin pane working directory | Working directory for execution. Literal relative paths join to the origin working directory. |
| `env` | mapping of strings or context | no | {} | Environment variables set for the child process. |


> [!NOTE]
> **Zellij:** Broker-owned supervised local process; no Zellij RPC
>
> **Herdr:** Broker-owned supervised local process; no Herdr RPC

### `tab:create`

Creates a new tab in the multiplexer.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `workspace-id` | string or context | no | captured origin workspace | Destination workspace identifier. |
| `name` | string or context | no | host default | Title or label for the new tab. |
| `focus` | boolean (literal only) | no | true | Whether to focus the new tab upon creation. Context references are rejected in v1. |
| `program` | string or context | no | host default shell | Executable to spawn in the initial pane of the new tab. |
| `args` | sequence of strings or context | no | [] | Arguments for program. Requires program to be set. |
| `cwd` | path or context | no | captured origin working directory | Initial working directory for the tab. |


> [!NOTE]
> **Zellij:** `NewTab` accepts `name`, `focus`, and exact `program`/`args`/`cwd`; `workspace-id` is rejected because Zellij has no workspaces. Focused creation waits for UI dismissal before bridge dispatch.
>
> **Herdr:** Without `program`, `tab.create` accepts `workspace-id`, `name` as `label`, `focus`, and `cwd`. Command tabs use `layout.apply`. Focused creation waits for the UI pane's retained `pane_closed` event.

### `tab:close`

Closes the current tab.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!NOTE]
> **Zellij:** `Action::CloseTab` or `close_focused_tab`; current focused tab only
>
> **Herdr:** `tab.close` on the captured origin tab

### `tab:rename`

Renames a tab.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string or context | no | — | New display name for the tab. Both Zellij and Herdr require name; bare interactive prompts are not supported by either host. |


> [!NOTE]
> **Zellij:** Name plus captured origin tab index maps to `RenameTab`; bare rename is rejected because Zellij has no prompt variant
>
> **Herdr:** `tab.rename` on the captured origin tab only when `name` is supplied; bare rename is incompatible

### `tab:focus`

Moves tab focus to a specified index or adjacent tab.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `index`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or context | no | — | Target tab index. Exactly one of index or direction is required. |
| `direction` | direction or context | no | — | Relative tab direction (next or previous). Exactly one of index or direction is required. |


> [!NOTE]
> **Zellij:** Partial: `GoToTab`, `GoToNextTab`, and `GoToPreviousTab`; only index and next/previous
>
> **Herdr:** Pending runtime candidate: resolve the requested target through captured-workspace `tab.list` before host focus; source mapping is not implemented

### `tab:move`

Moves the tab to a different position or in a specified direction.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `direction`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `direction` | direction or context | no | — | Relative shift direction. Exactly one of index or direction is required. |
| `index` | non-negative integer or context | no | — | Absolute destination index. Exactly one of index or direction is required. |


> [!NOTE]
> **Zellij:** Direction maps to `MoveTab` (accepts left, right, up, down); index is rejected because Zellij exposes no index destination primitive
>
> **Herdr:** `tab.move` accepts a concrete insert index on the captured origin tab; directional moves are rejected

### `tab:swap`

Swaps the positions of two tabs.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `index`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or context | no | — | Target tab index to swap with the origin tab. Exactly one of index or direction is required. |
| `direction` | direction or context | no | — | Relative tab direction (next or previous). Exactly one of index or direction is required. |


> [!WARNING]
> **Zellij:** Rejected: no atomic pinned primitive swaps two tabs
>
> **Herdr:** Nonnegative index only: list captured workspace, make two direct moves, and verify ordering; partial failure has no replay and a lost move response is OutcomeUnknown

### `pane:create`

Creates a new pane using host layout rules.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!WARNING]
> **Zellij:** `Action::NewPane` or `new_pane`; placement is host-determined in the focused tab
>
> **Herdr:** Incompatible; Herdr protocol 20 provides no automatic placement primitive

### `pane:split`

Splits the origin pane in a specified direction.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `direction`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `direction` | direction or context | no | host default | Split placement direction: left, right, up, or down. |
| `focus` | boolean (literal only) | no | true | Whether to focus the new pane. Context references are rejected in v1. |
| `program` | string or context | no | host default shell | Executable to spawn in the new pane. |
| `args` | sequence of strings or context | no | [] | Arguments for program. Requires program to be set. |
| `cwd` | path or context | no | captured origin working directory | Working directory for the new pane. |


> [!NOTE]
> **Zellij:** `NewTiledPane` accepts direction and exact `program`/`args`/`cwd`. Focused creation waits for UI dismissal before bridge dispatch; `focus: false` is rejected because Zellij cannot target the immutable origin while the menu is open.
>
> **Herdr:** Only explicit `right` or `down` directions are supported. Without `program`, `pane.split` also accepts `focus` and `cwd`. Command splits validate the origin with `session.snapshot`, then use `layout.apply` and `pane.move`. Focused creation waits for the UI pane's retained `pane_closed` event.

### `pane:close`

Closes the captured origin pane.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!NOTE]
> **Zellij:** `CloseFocusByPaneId` on the captured origin pane
>
> **Herdr:** `pane.close` on the captured origin pane

### `pane:focus`

Moves pane focus to an adjacent pane or specified index.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `index`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or context | no | — | Target pane manifest index. Exactly one of index or direction is required. Note that index is the positional field in compact syntax; direction requires direction=<dir>. |
| `direction` | direction or context | no | — | Direction of neighbor pane (left, right, up, or down). Exactly one of index or direction is required. |


> [!NOTE]
> **Zellij:** Direction uses bridge-tracked geometry and index uses bridge manifest order; neither falls back to menu-relative focus
>
> **Herdr:** `pane.focus_direction` on the captured origin pane for cardinal directions (left, right, up, down); indexed focus is rejected

### `pane:move`

Moves the captured origin pane in a specified direction or to a destination index.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `direction`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `direction` | direction or context | no | — | Direction to move: left, right, up, or down. Positional field in compact syntax. Exactly one of index or direction is required. |
| `index` | non-negative integer or context | no | — | Target destination index in the layout. Requires index=<index> in compact syntax. Exactly one of index or direction is required. |


> [!WARNING]
> **Zellij:** Direction maps to `MovePaneByPaneId` on the captured origin pane; index is rejected because no primitive exists
>
> **Herdr:** Incompatible: Herdr `pane.move` requires an explicit destination tab or new-tab container, not a portable index or direction

### `pane:swap`

Swaps the captured origin pane with an adjacent pane or specified index.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `index`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or context | no | — | Target pane index to swap with. Positional field in compact syntax. Exactly one of index or direction is required. |
| `direction` | direction or context | no | — | Direction of neighbor pane: left, right, up, or down. Requires direction=<dir> in compact syntax. Exactly one of index or direction is required. |


> [!WARNING]
> **Zellij:** Rejected: no pinned primitive swaps two panes
>
> **Herdr:** `pane.swap(origin_pane_id, direction)` only for cardinal directions (left, right, up, down); indexed swap is rejected

### `pane:resize`

Resizes the captured origin pane in a specified direction.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `direction` | direction or context | yes | — | Direction to resize: left, right, up, or down. |
| `amount` | scalar or context | no | host step | Resize magnitude. Omit for default single-step increase. |


> [!NOTE]
> **Zellij:** `ResizeByPaneId` on the captured origin pane; omitted amount increases, explicit amount is rejected
>
> **Herdr:** `pane.resize(origin_pane_id, direction, optional numeric amount)` only for left/right/up/down

### `pane:zoom`

Toggles whether the origin pane is zoomed within its layout.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean (literal only) | no | toggle | Explicit target state. Omit to toggle current state. Context references are rejected in v1. |


> [!NOTE]
> **Zellij:** Bare action maps to `ToggleFocusFullscreenByPaneId`; explicit boolean is rejected
>
> **Herdr:** `pane.zoom(origin_pane_id, mode toggle/on/off)`; boolean or omitted input

### `pane:fullscreen`

Toggles whether the origin pane occupies the full terminal without host chrome.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean (literal only) | no | toggle | Explicit target state. Omit to toggle current state. Context references are rejected in v1. |


> [!WARNING]
> **Zellij:** Bare action maps to `ToggleFocusNoUiFullscreenByPaneId`; explicit boolean is rejected
>
> **Herdr:** Incompatible; no protocol-20 method

### `pane:floating`

Toggles whether the origin pane floats above the tiled layout.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean (literal only) | no | toggle | Explicit target state. Omit to toggle current state. Context references are rejected in v1. |


> [!WARNING]
> **Zellij:** Bare action maps to `TogglePaneEmbedOrFloatingByPaneId`; explicit boolean is rejected
>
> **Herdr:** Incompatible; no protocol-20 method

### `pane:frame`

Toggles frame borders around the origin pane.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `visible` | boolean (literal only) | no | toggle | Explicit frame visibility. Omit to toggle current state. Context references are rejected in v1. |


> [!WARNING]
> **Zellij:** Rejected: `TogglePaneFrames` is menu-relative, not origin-pane targeted
>
> **Herdr:** Incompatible; no protocol-20 method

### `session:create`

Creates a new multiplexer session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!WARNING]
> **Zellij:** No one-to-one plugin API
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:attach`

Attaches a client connection to an existing session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `name`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string or context | yes | — | Name of the session to attach. |


> [!WARNING]
> **Zellij:** `switch_session(Some(name))` maps through active client session context
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:switch`

Switches the current client connection to another session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `name`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string or context | yes | — | Target session name. |


> [!WARNING]
> **Zellij:** `switch_session(Some(name))` changes client session context
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:rename`

Renames the active session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable
- **Positional field (compact):** `name`

#### Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string or context | yes | — | New name for the session. |


> [!WARNING]
> **Zellij:** `RenameSession` with name for current session context
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:detach`

Detaches the active client from the session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!WARNING]
> **Zellij:** `Detach` disconnects active client
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:quit`

Requests a clean shutdown of the session.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!WARNING]
> **Zellij:** Rejected: `quit_zellij()` terminates the server, not one portable session
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

### `session:kill`

Forcefully terminates the session and its contained processes.

- **Default mode:** `await`
- **Capabilities:** awaitable, detachable

#### Parameters

None.


> [!CAUTION]
> **Zellij:** `kill_sessions([captured origin session])`, distinct from server quit
>
> **Herdr:** Incompatible; only read-only `session.snapshot` exists

## Command working directory

The broker never runs `command:execute` in its ambient working directory. It uses the captured origin pane working directory as the execution base.

- With no `cwd`, the child uses the captured origin directory.
- A literal relative `cwd` is joined to the captured origin directory.
- A context-derived `cwd` must resolve to an absolute path.
- If the captured origin directory is absent or not absolute, the broker rejects the command before spawning it.

This rule applies to menu command execution. It does not define the omitted `muxe pane open --cwd` behavior.

## Origin context references

Context references resolve properties from the origin pane captured when opening a menu:

| Reference token | Value type | Description |
|---|---|---|
| `$origin.host.kind` | `HostKind` | Multiplexer type (`zellij` or `herdr`). |
| `$origin.server.id` | `ServerId` | Host server identifier. |
| `$origin.client.id` | `ClientId` | Client connection identifier. |
| `$origin.session.id` | `SessionId` | Multiplexer session identifier. |
| `$origin.workspace.id` | `WorkspaceId` | Active workspace identifier. |
| `$origin.tab.id` | `TabId` | Captured tab identifier. |
| `$origin.tab.index` | `UnsignedInteger` | Zero-based index of the captured tab. |
| `$origin.pane.id` | `PaneId` | Captured pane identifier. |
| `$origin.pane.type` | `PaneType` | Pane type (terminal or plugin). |
| `$origin.pane.cwd` | `AbsolutePath` | Absolute working directory of the origin pane. |
| `$origin.selection.text` | `String` | Current text selection in the origin pane. |
| `$origin.invocation.source` | `InvocationSource` | How the menu was triggered. |
| `$origin.worktree.id` | `WorktreeId` | Associated Git worktree identifier. |
| `$origin.worktree.path` | `AbsolutePath` | Path to the active Git worktree root. |
| `$origin.agent.id` | `AgentId` | Connected automation agent identifier. |
| `$origin.link.url` | `Url` | Hyperlink target under the cursor. |
| `$origin.link.handler.id` | `LinkHandlerId` | Associated link handler identifier. |

## Canonical key syntax

A canonical key has the form `[selector:]modifier+…+identity`. The selector is omitted for `primary`; `alternate` and `base` select the corresponding terminal identity.
Modifiers must appear once and in this order: `ctrl`, `alt`, `shift`, `super`, `hyper`, `meta`, `caps-lock`, `num-lock`.
Text identities use one printable ASCII character, except `+` and `:`, or `unicode+<hex-scalar>`. Named identities use the generated registry below. Examples: `ctrl+c`, `alternate:alt+unicode+e9`, and `ctrl+f12`.

## VT100 aliases

A VT100 terminal reports one legacy event for each of these pairs: `tab` and `ctrl+i`; `enter` and `ctrl+m`; `backspace` and `ctrl+h`; `esc` and `ctrl+[`.
For `ctrl+<letter>`, VT100 also cannot distinguish Shift or ASCII letter case. Muxe rejects a configuration that contains two bindings for one such legacy event. Kitty preserves the identities and modifiers it receives, so these bindings remain distinct there.
## Key capability rules

The generated table gives requirements for each named identity with the primary selector and no modifiers.
Applying `alternate:` or `base:` requires alternate-key support. An explicit `repeat:` setting requires event-type support. Lock modifiers require all-keys-as-escape-codes for text identities.
| Named identity | Base capabilities | With explicit `repeat:` |
|---|---|---|
| `esc` | none | event types |
| `enter` | none | event types, all keys as escape codes |
| `tab` | none | event types, all keys as escape codes |
| `backspace` | none | event types, all keys as escape codes |
| `insert` | none | event types |
| `delete` | none | event types |
| `left` | none | event types |
| `right` | none | event types |
| `up` | none | event types |
| `down` | none | event types |
| `home` | none | event types |
| `end` | none | event types |
| `pgup` | none | event types |
| `pgdn` | none | event types |
| `print-screen` | none | event types |
| `pause` | none | event types |
| `menu` | none | event types |
| `caps-lock` | none | event types |
| `scroll-lock` | none | event types |
| `num-lock` | none | event types |
| `f1` | none | event types |
| `f2` | none | event types |
| `f3` | none | event types |
| `f4` | none | event types |
| `f5` | none | event types |
| `f6` | none | event types |
| `f7` | none | event types |
| `f8` | none | event types |
| `f9` | none | event types |
| `f10` | none | event types |
| `f11` | none | event types |
| `f12` | none | event types |
| `f13` | none | event types |
| `f14` | none | event types |
| `f15` | none | event types |
| `f16` | none | event types |
| `f17` | none | event types |
| `f18` | none | event types |
| `f19` | none | event types |
| `f20` | none | event types |
| `f21` | none | event types |
| `f22` | none | event types |
| `f23` | none | event types |
| `f24` | none | event types |
| `f25` | none | event types |
| `f26` | none | event types |
| `f27` | none | event types |
| `f28` | none | event types |
| `f29` | none | event types |
| `f30` | none | event types |
| `f31` | none | event types |
| `f32` | none | event types |
| `f33` | none | event types |
| `f34` | none | event types |
| `f35` | none | event types |
| `keypad+0` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+1` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+2` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+3` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+4` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+5` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+6` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+7` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+8` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+9` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+decimal` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+divide` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+multiply` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+subtract` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+add` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+enter` | none | event types |
| `keypad+equal` | none | event types |
| `keypad+separator` | none | event types |
| `keypad+left` | none | event types |
| `keypad+right` | none | event types |
| `keypad+up` | none | event types |
| `keypad+down` | none | event types |
| `keypad+pgup` | none | event types |
| `keypad+pgdn` | none | event types |
| `keypad+home` | none | event types |
| `keypad+end` | none | event types |
| `keypad+insert` | none | event types |
| `keypad+delete` | none | event types |
| `keypad+begin` | none | event types |
| `media-play` | none | event types |
| `media-pause` | none | event types |
| `media-play-pause` | none | event types |
| `media-reverse` | none | event types |
| `media-stop` | none | event types |
| `media-fast-forward` | none | event types |
| `media-rewind` | none | event types |
| `media-track-next` | none | event types |
| `media-track-previous` | none | event types |
| `media-record` | none | event types |
| `lower-volume` | none | event types |
| `raise-volume` | none | event types |
| `mute-volume` | none | event types |
| `left-shift` | all keys as escape codes | event types, all keys as escape codes |
| `left-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `left-alt` | all keys as escape codes | event types, all keys as escape codes |
| `left-super` | all keys as escape codes | event types, all keys as escape codes |
| `left-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `left-meta` | all keys as escape codes | event types, all keys as escape codes |
| `right-shift` | all keys as escape codes | event types, all keys as escape codes |
| `right-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `right-alt` | all keys as escape codes | event types, all keys as escape codes |
| `right-super` | all keys as escape codes | event types, all keys as escape codes |
| `right-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `right-meta` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level3-shift` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level5-shift` | all keys as escape codes | event types, all keys as escape codes |

## Native actions

A native action uses a `type` beginning with `native.`. The core retains its source-aware candidate fields, but does not interpret the host schema. The active adapter validates the native type and fields before activation. Example: `native.zellij.command:close-focus`. Native-action availability is therefore adapter evidence, not a claim in this reference.

# Muxe core reference

> Generated by `cargo run --locked -p muxe-core --bin generate_reference -- REFERENCE.md reference/host-support.tsv`. Do not edit this file by hand.

The key and portable-action tables are generated from `muxe-core`. The host table is generated from checked-in adapter evidence. Every host claim carries its evidence status.

## Scope and evidence

Portable actions are host-independent candidates. An active adapter validates a candidate before configuration activation, and an adapter may reject an action that its host cannot represent.
Context references are type-checked while compiling configuration. Muxe resolves them against the attach-time origin and validates the concrete value again before dispatch. A missing context value prevents dispatch.

## Action schema

Use either a YAML mapping with `type` or a compact action string. In compact form, an unquoted `$origin…` token is a typed context reference; quote it to make it literal. A mapping context reference uses `{ $context: origin.… }`.

```yaml
action:
  type: command:execute
  program: cargo
  cwd:
    $context: origin.pane.cwd

# Compact form
action: tab:create workspace-id=$origin.workspace.id
```

| Portable action | Accepted fields | Default mode | Capabilities | Evidence |
|---|---|---|---|---|
| `menu:open` | menu=<id> or submenu=<menu> | — | none | Core source only |
| `menu:return` | none | — | none | Core source only |
| `menu:quit` | none | — | none | Core source only |
| `menu.page:prev` | none | — | none | Core source only |
| `menu.page:next` | none | — | none | Core source only |
| `config:reload` | none | — | none | Core source only |
| `keyboard:send` | exactly one of keys=[<canonical-key or $context>, …] or text=<string or $context> | await | awaitable, detachable | Core source only |
| `command:execute` | program=<string or $context>; args=[<string or $context>, …]?; cwd=<path or $context>?; env={<string>: <string or $context>}? | detach | awaitable, detachable, cancellable | Core source only |
| `tab:create` | workspace-id=<string or $context>? | await | awaitable, detachable | Core source only |
| `tab:close` | none | await | awaitable, detachable | Core source only |
| `tab:rename` | name=<string or $context>? | await | awaitable, detachable | Core source only |
| `tab:focus` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `tab:move` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `tab:swap` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `pane:create` | none | await | awaitable, detachable | Core source only |
| `pane:split` | direction=<direction or $context>? | await | awaitable, detachable | Core source only |
| `pane:close` | none | await | awaitable, detachable | Core source only |
| `pane:focus` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `pane:move` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `pane:swap` | exactly one of index=<non-negative integer or $context> or direction=<direction or $context> | await | awaitable, detachable | Core source only |
| `pane:resize` | direction=<direction or $context>; amount=<scalar or $context>? | await | awaitable, detachable | Core source only |
| `pane:zoom` | enabled=<boolean>? | await | awaitable, detachable | Core source only |
| `pane:fullscreen` | enabled=<boolean>? | await | awaitable, detachable | Core source only |
| `pane:floating` | enabled=<boolean>? | await | awaitable, detachable | Core source only |
| `pane:frame` | visible=<boolean>? | await | awaitable, detachable | Core source only |
| `session:create` | none | await | awaitable, detachable | Core source only |
| `session:attach` | name=<string or $context> | await | awaitable, detachable | Core source only |
| `session:switch` | name=<string or $context> | await | awaitable, detachable | Core source only |
| `session:rename` | name=<string or $context> | await | awaitable, detachable | Core source only |
| `session:detach` | none | await | awaitable, detachable | Core source only |
| `session:quit` | none | await | awaitable, detachable | Core source only |
| `session:kill` | none | await | awaitable, detachable | Core source only |

## Command working directory

The broker never runs `command:execute` in its ambient working directory. It uses the captured origin pane working directory as the execution base.

- With no `cwd`, the child uses the captured origin directory.
- A literal relative `cwd` is joined to the captured origin directory.
- A context-derived `cwd` must resolve to an absolute path.
- If the captured origin directory is absent or not absolute, the broker rejects the command before spawning it.

This rule applies to menu command execution. It does not define the omitted `muxe pane open --cwd` behavior.

## Host support evidence

- **E1 — owned-fixture smoke:** An approved fresh-TempDir Herdr child completed `ping`, observed `Herdr` at its exact configured socket with protocol 20/version 0.8.2, then terminated gracefully and reaped its retained child. It did not smoke a portable action.
- **E2 — Herdr source evidence:** Adapter mapping and schema tests passed. The mappings below are source-tested, not live action tests.
- **E3 — Zellij source evidence:** Pinned 0.46.0 conversion and inventory checks passed. No active Zellij host smoke has run.

| Portable action | Zellij mapping and constraints (E3) | Herdr mapping and constraints (E1/E2) |
|---|---|---|
| `menu:open` | Muxe menu state; no Zellij Action or shim equivalent | Broker UI control; no Herdr RPC |
| `menu:return` | Muxe menu state; no Zellij Action or shim equivalent | Broker UI control; no Herdr RPC |
| `menu:quit` | Muxe menu state; no Zellij Action or shim equivalent | Broker UI control; no Herdr RPC |
| `menu.page:prev` | Muxe menu state; no Zellij Action or shim equivalent | Broker UI control; no Herdr RPC |
| `menu.page:next` | Muxe menu state; no Zellij Action or shim equivalent | Broker UI control; no Herdr RPC |
| `config:reload` | No equivalent; `shim::reconfigure` requires a Zellij config payload | Broker configuration control; no Herdr RPC |
| `keyboard:send` | `Action::Write` or `Action::WriteChars` candidate; current focus only, no target-pane field | `pane.send_keys` or `pane.send_text` to the captured origin pane; keys/text must be strings |
| `command:execute` | Not exposed; `run_command*` is classified as background/unsupported | Broker-owned supervised local process; no adapter RPC |
| `tab:create` | No exact mapping; `NewTab` and `new_tab` use name/cwd/layout semantics, not workspace ID | `tab.create(workspace_id or null)` |
| `tab:close` | `Action::CloseTab` or `close_focused_tab`; current focused tab only | `tab.close` on the captured origin tab |
| `tab:rename` | No exact mapping; Zellij requires a tab index, which the portable action lacks | `tab.rename` on the captured origin tab only when `name` is supplied; bare rename is incompatible |
| `tab:focus` | Partial: `GoToTab`, `GoToNextTab`, and `GoToPreviousTab`; only index and next/previous | Incompatible; Herdr requires a tab ID while the portable action provides index/direction |
| `tab:move` | Partial: `MoveTab { direction }`; no portable index destination | `tab.move(origin_tab_id, insert_index)` only for a nonnegative index; direction is incompatible |
| `tab:swap` | No one-to-one pinned Action or shim operation | Nonnegative index only: list captured workspace, make two direct moves, and verify ordering; partial failure has no replay and a lost move response is OutcomeUnknown |
| `pane:create` | `Action::NewPane` or `new_pane`; placement is host-determined in the focused tab | Incompatible |
| `pane:split` | `NewTiledPane` or `NewPane`; adapter must supply nonportable fields and uses the current tab | `pane.split(origin_pane_id, direction)` only for explicit `right` or `down`; no inferred direction |
| `pane:close` | `Action::CloseFocus` or `close_focus`; current focused pane only | `pane.close` on the captured origin pane |
| `pane:focus` | Partial: `MoveFocus`, `FocusNextPane`, and `FocusPreviousPane`; no index without a pane inventory query | `pane.focus_direction` on the origin pane only for left/right/up/down |
| `pane:move` | Partial: `MovePane { direction }`; no portable index destination | Incompatible; Herdr needs an explicit tab or new-tab destination |
| `pane:swap` | No one-to-one pinned operation | `pane.swap(origin_pane_id, direction)` only for left/right/up/down |
| `pane:resize` | Partial: `Action::Resize`; no portable scalar amount semantics | `pane.resize(origin_pane_id, direction, optional numeric amount)` only for left/right/up/down |
| `pane:zoom` | Not idempotent; only fullscreen toggles are available | `pane.zoom(origin_pane_id, mode toggle/on/off)`; boolean or omitted input |
| `pane:fullscreen` | Not exposed as idempotent; only a fullscreen toggle exists | Incompatible; no protocol-20 method |
| `pane:floating` | Not exposed as idempotent; only embed/floating toggles exist | Incompatible; no protocol-20 method |
| `pane:frame` | No exact boolean mapping; `SetPaneFrameStyle` requires a style | Incompatible; no protocol-20 method |
| `session:create` | No one-to-one plugin API | Incompatible; only read-only `session.snapshot` exists |
| `session:attach` | No one-to-one plugin API | Incompatible; only read-only `session.snapshot` exists |
| `session:switch` | `switch_session(Some(name))` candidate; changes plugin/client session context | Incompatible; only read-only `session.snapshot` exists |
| `session:rename` | `rename_session(name)` candidate; current session context | Incompatible; only read-only `session.snapshot` exists |
| `session:detach` | `detach()` candidate; client/plugin context dependent | Incompatible; only read-only `session.snapshot` exists |
| `session:quit` | Not exposed; `quit_zellij()` terminates the server, not a portable session | Incompatible; only read-only `session.snapshot` exists |
| `session:kill` | Not exposed; `kill_sessions` requires explicit names | Incompatible; only read-only `session.snapshot` exists |

## Canonical key syntax

A canonical key has the form `[selector:]modifier+…+identity`. The selector is omitted for `primary`; `alternate` and `base` select the corresponding terminal identity.
Modifiers must appear once and in this order: `ctrl`, `alt`, `shift`, `super`, `hyper`, `meta`, `caps-lock`, `num-lock`.
Text identities use one printable ASCII character, except `+` and `:`, or `unicode+<hex-scalar>`. Named identities use the generated registry below. Examples: `ctrl+c`, `alternate:alt+unicode+e9`, and `ctrl+f12`.

## VT100 aliases

A VT100 terminal reports one legacy event for each of these pairs: `tab` and `ctrl+i`; `enter` and `ctrl+m`; `backspace` and `ctrl+h`; `esc` and `ctrl+[`.
For `ctrl+<letter>`, VT100 also cannot distinguish Shift or ASCII letter case. Muxe rejects a configuration that contains two bindings for one such legacy event. Kitty preserves the identities and modifiers it receives, so these bindings remain distinct there.
## Key capability rules

The generated table gives requirements for each named identity with the primary selector and no modifiers.
Applying `alternate:` or `base:` requires alternate-key support. An explicit `repeat:` setting requires event-type support. Lock modifiers require all-keys-as-escape-codes for text identities.
| Named identity | Base capabilities | With explicit `repeat:` |
|---|---|---|
| `esc` | none | event types |
| `enter` | none | event types, all keys as escape codes |
| `tab` | none | event types, all keys as escape codes |
| `backspace` | none | event types, all keys as escape codes |
| `insert` | none | event types |
| `delete` | none | event types |
| `left` | none | event types |
| `right` | none | event types |
| `up` | none | event types |
| `down` | none | event types |
| `home` | none | event types |
| `end` | none | event types |
| `pgup` | none | event types |
| `pgdn` | none | event types |
| `print-screen` | none | event types |
| `pause` | none | event types |
| `menu` | none | event types |
| `caps-lock` | none | event types |
| `scroll-lock` | none | event types |
| `num-lock` | none | event types |
| `f1` | none | event types |
| `f2` | none | event types |
| `f3` | none | event types |
| `f4` | none | event types |
| `f5` | none | event types |
| `f6` | none | event types |
| `f7` | none | event types |
| `f8` | none | event types |
| `f9` | none | event types |
| `f10` | none | event types |
| `f11` | none | event types |
| `f12` | none | event types |
| `f13` | none | event types |
| `f14` | none | event types |
| `f15` | none | event types |
| `f16` | none | event types |
| `f17` | none | event types |
| `f18` | none | event types |
| `f19` | none | event types |
| `f20` | none | event types |
| `f21` | none | event types |
| `f22` | none | event types |
| `f23` | none | event types |
| `f24` | none | event types |
| `f25` | none | event types |
| `f26` | none | event types |
| `f27` | none | event types |
| `f28` | none | event types |
| `f29` | none | event types |
| `f30` | none | event types |
| `f31` | none | event types |
| `f32` | none | event types |
| `f33` | none | event types |
| `f34` | none | event types |
| `f35` | none | event types |
| `keypad+0` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+1` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+2` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+3` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+4` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+5` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+6` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+7` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+8` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+9` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+decimal` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+divide` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+multiply` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+subtract` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+add` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+enter` | none | event types |
| `keypad+equal` | none | event types |
| `keypad+separator` | none | event types |
| `keypad+left` | none | event types |
| `keypad+right` | none | event types |
| `keypad+up` | none | event types |
| `keypad+down` | none | event types |
| `keypad+pgup` | none | event types |
| `keypad+pgdn` | none | event types |
| `keypad+home` | none | event types |
| `keypad+end` | none | event types |
| `keypad+insert` | none | event types |
| `keypad+delete` | none | event types |
| `keypad+begin` | none | event types |
| `media-play` | none | event types |
| `media-pause` | none | event types |
| `media-play-pause` | none | event types |
| `media-reverse` | none | event types |
| `media-stop` | none | event types |
| `media-fast-forward` | none | event types |
| `media-rewind` | none | event types |
| `media-track-next` | none | event types |
| `media-track-previous` | none | event types |
| `media-record` | none | event types |
| `lower-volume` | none | event types |
| `raise-volume` | none | event types |
| `mute-volume` | none | event types |
| `left-shift` | all keys as escape codes | event types, all keys as escape codes |
| `left-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `left-alt` | all keys as escape codes | event types, all keys as escape codes |
| `left-super` | all keys as escape codes | event types, all keys as escape codes |
| `left-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `left-meta` | all keys as escape codes | event types, all keys as escape codes |
| `right-shift` | all keys as escape codes | event types, all keys as escape codes |
| `right-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `right-alt` | all keys as escape codes | event types, all keys as escape codes |
| `right-super` | all keys as escape codes | event types, all keys as escape codes |
| `right-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `right-meta` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level3-shift` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level5-shift` | all keys as escape codes | event types, all keys as escape codes |

## Native actions

A native action uses a `type` beginning with `native.`. The core retains its source-aware candidate fields, but does not interpret the host schema. The active adapter validates the native type and fields before activation. Native-action availability is therefore adapter evidence, not a claim in this reference.

# Muxe action reference

> Generated by `cargo run --locked -p muxe-core --bin generate_reference -- REFERENCE.md reference/host-support.tsv reference/action-descriptions.yaml`. Do not edit this file by hand.

Use actions in menu bindings to tell Muxe what to do when you press a key. Actions can open another menu, run a command, or control a tab, pane, or session.

## How Muxe handles actions

When Muxe loads its configuration, it checks each action name, parameter, and value. It also checks whether Zellij or Herdr supports the requested operation. If an action is invalid or unsupported, Muxe reports an error and does not load that configuration.

Muxe remembers which session, tab, and pane were active when you opened the menu. In compact actions, references such as `$origin.pane.cwd` read values from that saved location. The mapping form writes the same reference as `{ $context: origin.pane.cwd }`.

Muxe rejects the configuration if a reference has a type that the parameter cannot use. Some saved values are optional and may be unavailable. If an optional value is unavailable when the binding runs, Muxe reports an error for that action and leaves the configuration loaded.

## Writing an action

Write an action as a YAML mapping or as a compact string. The mapping form supports strings, lists, mappings, and `$context` references. The compact form accepts an action name followed by positional or `name=value` arguments.

In compact form, an unquoted value that begins with `$origin.` is a reference. Quote the value when you need to pass the `$` text unchanged.

```yaml
# Mapping form
action:
  type: command:execute
  program: cargo
  cwd:
    $context: origin.pane.cwd

# Compact form
action: command:execute program=cargo cwd=$origin.pane.cwd
```

`await` waits for an action to finish. `detach` starts it without waiting. Cancellation stops a running action when an execution timeout or menu-control rule requests it. Each action below lists the supported choices.

## Action index

| Category | Actions |
|---|---|
| Menu | [`menu:open`](#menuopen), [`menu:return`](#menureturn), [`menu:quit`](#menuquit), [`menu.page:prev`](#menupageprev), [`menu.page:next`](#menupagenext) |
| Configuration | [`config:reload`](#configreload) |
| Keyboard | [`keyboard:send`](#keyboardsend) |
| Command | [`command:execute`](#commandexecute) |
| Tab | [`tab:create`](#tabcreate), [`tab:close`](#tabclose), [`tab:rename`](#tabrename), [`tab:focus`](#tabfocus), [`tab:move`](#tabmove), [`tab:swap`](#tabswap) |
| Pane | [`pane:create`](#panecreate), [`pane:split`](#panesplit), [`pane:close`](#paneclose), [`pane:focus`](#panefocus), [`pane:move`](#panemove), [`pane:swap`](#paneswap), [`pane:resize`](#paneresize), [`pane:zoom`](#panezoom), [`pane:fullscreen`](#panefullscreen), [`pane:floating`](#panefloating), [`pane:frame`](#paneframe) |
| Session | [`session:create`](#sessioncreate), [`session:attach`](#sessionattach), [`session:switch`](#sessionswitch), [`session:rename`](#sessionrename), [`session:detach`](#sessiondetach), [`session:quit`](#sessionquit), [`session:kill`](#sessionkill) |

## Actions


### `menu:open`

Opens a named menu or an inline submenu.

- **Execution:** immediate.
- **Compact form:** `menu` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `menu` | menu name | exactly one of `menu` or `submenu` | — | Name of a top-level menu in the configuration. `$origin.` references are not supported. |
| `submenu` | menu mapping | exactly one of `menu` or `submenu` | — | Menu definition written directly inside the action. |

### `menu:return`

Returns to the previous menu. If there is no previous menu, closes the menu.

- **Execution:** immediate.

#### Parameters

This action has no parameters.

### `menu:quit`

Closes the menu and returns keyboard focus to the pane where you opened it.

- **Execution:** immediate.

#### Parameters

This action has no parameters.

### `menu.page:prev`

Shows the previous page of menu items.

- **Execution:** immediate.

#### Parameters

This action has no parameters.

### `menu.page:next`

Shows the next page of menu items.

- **Execution:** immediate.

#### Parameters

This action has no parameters.

### `config:reload`

Reloads the Muxe configuration files.

- **Execution:** immediate.

#### Parameters

This action has no parameters.

### `keyboard:send`

Sends keys or text to the pane where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `keys` | list of keys or `$origin.` references | exactly one of `keys` or `text` | — | Keys to send in order, such as `["ctrl+c", "enter"]`. |
| `text` | string or string-valued `$origin.` reference | exactly one of `keys` or `text` | — | Text to send unchanged. |

> [!WARNING]
> **Zellij:** Supports text, control characters, Alt combinations, common navigation keys, and function keys through F12. It does not support Super or Hyper combinations, keypad or media keys, or function keys F13 and above.

### `command:execute`

Starts a command on the local machine.

- **Execution:** `detach` by default; `await` and cancellation supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `program` | string or `$origin.` reference | required | — | Executable name or path. |
| `args` | list of strings or `$origin.` references | optional | empty list | Arguments passed to the program. |
| `cwd` | path or `$origin.` reference | optional | working directory of the pane where the menu was opened | Directory in which to start the command. A relative path starts from the working directory of the pane where you opened the menu. |
| `env` | mapping | optional | empty mapping | Environment variables for the command. Each value may be a string or a `$origin.` reference. |

### `tab:create`

Creates a tab.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `workspace-id` | workspace ID or `$origin.` reference | optional | workspace containing the pane where the menu was opened | Workspace in which to create the tab. |
| `name` | string or `$origin.` reference | optional | chosen by Zellij or Herdr | Name shown for the new tab. |
| `focus` | boolean | optional | true | Set to `false` to leave focus on the tab where you opened the menu. `$origin.` references are not supported. |
| `program` | string or `$origin.` reference | optional | default shell | Program to start in the first pane. |
| `args` | list of strings or `$origin.` references | optional; requires `program` | empty list | Arguments passed to `program`. |
| `cwd` | path or `$origin.` reference | optional | working directory of the pane where the menu was opened | Working directory of the first pane. |

> [!WARNING]
> **Zellij:** Does not support `workspace-id`. All other parameters are supported.

### `tab:close`

Closes the tab where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

### `tab:rename`

Renames the tab where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `name` | string or `$origin.` reference | optional | — | New tab name. |

> [!WARNING]
> **Zellij:** Requires `name`; Zellij cannot prompt for a name.
>
> **Herdr:** Requires `name`; Herdr cannot prompt for a name.

### `tab:focus`

Focuses another tab.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `index` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Index of the tab to focus. |
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Relative tab direction. |

> [!WARNING]
> **Zellij:** Supports `index`, `next`, and `previous`. It does not support `left`, `right`, `up`, or `down`.
>
> **Herdr:** Not supported.

### `tab:move`

Moves the tab where you opened the menu to another position.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `direction` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Direction in which to move the tab. |
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Position to which to move the tab. |

> [!WARNING]
> **Zellij:** Supports `direction` with `left`, `right`, `up`, or `down`. It does not support `index`.
>
> **Herdr:** Supports `index`. It does not support `direction`.

### `tab:swap`

Exchanges the tab where you opened the menu with another tab.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `index` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Index of the other tab. |
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Direction of the other tab. |

> [!WARNING]
> **Zellij:** Not supported.
>
> **Herdr:** Supports `index`. Herdr performs the swap as two moves. If the second move fails, Herdr does not undo the first move.

### `pane:create`

Requests a new pane without specifying its position.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

> [!WARNING]
> **Zellij:** Supported. Zellij chooses where to place the pane.
>
> **Herdr:** Not supported. Use `pane:split` with `right` or `down`.

### `pane:split`

Splits the pane where you opened the menu.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `direction` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `direction` | direction or `$origin.` reference | optional | host-dependent; see host note | Side on which to create the new pane. |
| `focus` | boolean | optional | true | Set to `false` to leave focus on the existing pane. `$origin.` references are not supported. |
| `program` | string or `$origin.` reference | optional | default shell | Program to start in the new pane. |
| `args` | list of strings or `$origin.` references | optional; requires `program` | empty list | Arguments passed to `program`. |
| `cwd` | path or `$origin.` reference | optional | working directory of the pane where the menu was opened | Working directory of the new pane. |

> [!WARNING]
> **Zellij:** `direction` may be omitted; Zellij then chooses placement. All four directions are supported. `focus: false` is not supported. A `cwd` without `program` is not supported.
>
> **Herdr:** Requires `direction`, which must be `right` or `down`.

### `pane:close`

Closes the pane where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

### `pane:focus`

Focuses another pane.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `index` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Index of the pane to focus. |
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Direction of the pane to focus. |

> [!WARNING]
> **Zellij:** Supports `index` and all four directions.
>
> **Herdr:** Supports all four directions. It does not support `index`.

### `pane:move`

Moves the pane where you opened the menu.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `direction` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Direction in which to move the pane. |
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Position to which to move the pane. |

> [!WARNING]
> **Zellij:** Supports all four directions. It does not support `index`.
>
> **Herdr:** Not supported.

### `pane:swap`

Exchanges the pane where you opened the menu with another pane.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `index` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `index` | non-negative integer or `$origin.` reference | exactly one of `index` or `direction` | — | Index of the other pane. |
| `direction` | direction or `$origin.` reference | exactly one of `index` or `direction` | — | Direction of the other pane. |

> [!WARNING]
> **Zellij:** Not supported.
>
> **Herdr:** Supports all four directions. It does not support `index`.

### `pane:resize`

Changes the size of the pane where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `direction` | direction or `$origin.` reference | required | — | Edge to move. Use `left`, `right`, `up`, or `down`. |
| `amount` | number or `$origin.` reference | optional | one resize step | Distance by which to move the selected edge. |

> [!WARNING]
> **Zellij:** Supports a direction but not `amount`. Each action changes the pane by one Zellij resize step.
>
> **Herdr:** Supports all four directions and an optional numeric `amount`.

### `pane:zoom`

Makes one pane fill the tab's pane area, or restores the tab's layout.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean | optional | toggle the current state | Set to `true` to zoom or `false` to restore the layout. `$origin.` references are not supported. |

> [!WARNING]
> **Zellij:** Can only toggle the current state. Do not set `enabled`.
>
> **Herdr:** Can toggle the current state or set `enabled` to `true` or `false`.

### `pane:fullscreen`

Makes one pane fill the terminal, or restores the Zellij interface.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean | optional | toggle the current state | Set to `true` to enter fullscreen or `false` to leave it. `$origin.` references are not supported. |

> [!WARNING]
> **Zellij:** Can only toggle the current state. Do not set `enabled`.
>
> **Herdr:** Not supported.

### `pane:floating`

Moves a pane between the tiled layout and the floating layer.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `enabled` | boolean | optional | toggle the current state | Set to `true` for a floating pane or `false` for a tiled pane. `$origin.` references are not supported. |

> [!WARNING]
> **Zellij:** Can only toggle the current state. Do not set `enabled`.
>
> **Herdr:** Not supported.

### `pane:frame`

Shows or hides pane borders.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `visible` | boolean | optional | toggle the current state | Set to `true` to show borders or `false` to hide them. `$origin.` references are not supported. |

> [!WARNING]
> **Zellij:** Not supported.
>
> **Herdr:** Not supported.

### `session:create`

Requests a new session.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

> [!WARNING]
> **Zellij:** Not supported.
>
> **Herdr:** Not supported.

### `session:attach`

Connects the terminal running Muxe to an existing session.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `name` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `name` | string or `$origin.` reference | required | — | Name of the session. |

> [!WARNING]
> **Zellij:** Supported. Zellij moves this terminal connection to the named session.
>
> **Herdr:** Not supported.

### `session:switch`

Moves the terminal running Muxe from its session to another session.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `name` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `name` | string or `$origin.` reference | required | — | Name of the destination session. |

> [!WARNING]
> **Zellij:** Supported.
>
> **Herdr:** Not supported.

### `session:rename`

Renames the session where you opened the menu.

- **Execution:** `await` by default; `detach` supported.
- **Compact form:** `name` may be the first unnamed argument.

#### Parameters

| Parameter | Type | Requirement | Default | Description |
|---|---|---|---|---|
| `name` | string or `$origin.` reference | required | — | New session name. |

> [!WARNING]
> **Zellij:** Supported.
>
> **Herdr:** Not supported.

### `session:detach`

Disconnects the terminal running Muxe from the session where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

> [!WARNING]
> **Zellij:** Supported.
>
> **Herdr:** Not supported.

### `session:quit`

Requests a clean close of the session where you opened the menu.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

> [!WARNING]
> **Zellij:** Not supported. Zellij can stop the entire server, but it cannot close only the session where you opened the menu through this action.
>
> **Herdr:** Not supported.

### `session:kill`

Immediately stops the session where you opened the menu and the programs running in it.

- **Execution:** `await` by default; `detach` supported.

#### Parameters

This action has no parameters.

> [!CAUTION]
> **Zellij:** Terminates the session in which the menu was opened, including the programs running in that session.
>
> **Herdr:** Not supported.

## Command working directories

By default, `command:execute` runs in the working directory of the pane where you opened the menu.

- An absolute `cwd` uses that exact directory.
- A relative `cwd` starts from the pane's working directory.
- A `$origin.` reference used for `cwd` must resolve to an absolute path.
- If Muxe cannot determine an absolute working directory, it reports an error without starting the command.

## Using values from the pane that opened the menu

Use these `$origin.` references to reuse information from the pane where you opened the menu. Some optional references may be unavailable.

| Reference | Value | Description |
|---|---|---|
| `$origin.host.kind` | `zellij` or `herdr` | Terminal multiplexer where the menu was opened. |
| `$origin.server.id` | server ID | Zellij or Herdr server where the menu was opened. |
| `$origin.client.id` | client ID | Terminal connection that opened the menu. |
| `$origin.session.id` | session ID | Session where the menu was opened. |
| `$origin.workspace.id` | workspace ID | Workspace containing the pane. |
| `$origin.tab.id` | tab ID | Tab where the menu was opened. |
| `$origin.tab.index` | non-negative integer | Index of that tab. |
| `$origin.pane.id` | pane ID | Pane where the menu was opened. |
| `$origin.pane.type` | `tiled`, `floating`, `plugin`, `terminal`, or `other` | Type of that pane. |
| `$origin.pane.cwd` | absolute path | Working directory of that pane. |
| `$origin.selection.text` | string | Text selected in that pane. |
| `$origin.invocation.source` | `rootbinding`, `commandline`, `link`, or `automation` | How the menu was opened. |
| `$origin.worktree.id` | worktree ID | Git worktree associated with that pane. |
| `$origin.worktree.path` | absolute path | Path to that Git worktree. |
| `$origin.agent.id` | agent ID | Coding agent associated with that pane. |
| `$origin.link.url` | URL | URL under the cursor. |
| `$origin.link.handler.id` | link handler ID | Muxe link handler selected for that URL. |

## Key names

Keys use the form `[selector:]modifier+…+key`. Most keys omit the selector. Use `alternate:` or `base:` only when you need to select one of those terminal key representations.

Write modifiers in this order: `ctrl`, `alt`, `shift`, `super`, `hyper`, `meta`, `caps-lock`, `num-lock`.

For a printable character, write the character itself. Use `unicode+<hex-scalar>` for other Unicode characters. Examples: `ctrl+c`, `alternate:alt+unicode+e9`, and `ctrl+f12`.

### Lock modifier matching

Ordinary modifiers must match exactly. Each lock modifier in a binding is a requirement on the lock state the terminal reports: `caps-lock+left` matches only while Caps Lock is active. A binding with no lock modifier matches regardless of lock state, so `left` still matches when Caps Lock or Num Lock is active. Extra locks in the event do not disqualify a binding, while an extra ordinary modifier does. Lock modifiers on printable characters require all-keys-as-escape-codes mode.

### Keys that share a VT100 code

Older VT100-style terminal input cannot distinguish these pairs:

- `tab` and `ctrl+i`
- `enter` and `ctrl+m`
- `backspace` and `ctrl+h`
- `esc` and `ctrl+[`

VT100 input also cannot distinguish uppercase and lowercase letters while Ctrl is held. Muxe rejects bindings that would produce the same VT100 input. Terminals using the Kitty keyboard protocol can distinguish these keys.

### Terminal requirements for named keys

Some named keys need optional Kitty keyboard features. The table shows which features must be enabled. `none` means that the key needs no optional feature.

Using `alternate:` or `base:` requires alternate-key reporting. Setting `repeat` to either `true` or `false` requires key-event reporting. Lock modifiers on printable characters require all-keys-as-escape-codes mode.

| Named key | Without explicit `repeat` | With explicit `repeat` |
|---|---|---|
| `esc` | none | event types |
| `enter` | none | event types, all keys as escape codes |
| `tab` | none | event types, all keys as escape codes |
| `backspace` | none | event types, all keys as escape codes |
| `insert` | none | event types |
| `delete` | none | event types |
| `left` | none | event types |
| `right` | none | event types |
| `up` | none | event types |
| `down` | none | event types |
| `home` | none | event types |
| `end` | none | event types |
| `pgup` | none | event types |
| `pgdn` | none | event types |
| `print-screen` | none | event types |
| `pause` | none | event types |
| `menu` | none | event types |
| `caps-lock` | none | event types |
| `scroll-lock` | none | event types |
| `num-lock` | none | event types |
| `f1` | none | event types |
| `f2` | none | event types |
| `f3` | none | event types |
| `f4` | none | event types |
| `f5` | none | event types |
| `f6` | none | event types |
| `f7` | none | event types |
| `f8` | none | event types |
| `f9` | none | event types |
| `f10` | none | event types |
| `f11` | none | event types |
| `f12` | none | event types |
| `f13` | none | event types |
| `f14` | none | event types |
| `f15` | none | event types |
| `f16` | none | event types |
| `f17` | none | event types |
| `f18` | none | event types |
| `f19` | none | event types |
| `f20` | none | event types |
| `f21` | none | event types |
| `f22` | none | event types |
| `f23` | none | event types |
| `f24` | none | event types |
| `f25` | none | event types |
| `f26` | none | event types |
| `f27` | none | event types |
| `f28` | none | event types |
| `f29` | none | event types |
| `f30` | none | event types |
| `f31` | none | event types |
| `f32` | none | event types |
| `f33` | none | event types |
| `f34` | none | event types |
| `f35` | none | event types |
| `keypad+0` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+1` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+2` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+3` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+4` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+5` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+6` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+7` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+8` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+9` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+decimal` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+divide` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+multiply` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+subtract` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+add` | all keys as escape codes | event types, all keys as escape codes |
| `keypad+enter` | none | event types |
| `keypad+equal` | none | event types |
| `keypad+separator` | none | event types |
| `keypad+left` | none | event types |
| `keypad+right` | none | event types |
| `keypad+up` | none | event types |
| `keypad+down` | none | event types |
| `keypad+pgup` | none | event types |
| `keypad+pgdn` | none | event types |
| `keypad+home` | none | event types |
| `keypad+end` | none | event types |
| `keypad+insert` | none | event types |
| `keypad+delete` | none | event types |
| `keypad+begin` | none | event types |
| `media-play` | none | event types |
| `media-pause` | none | event types |
| `media-play-pause` | none | event types |
| `media-reverse` | none | event types |
| `media-stop` | none | event types |
| `media-fast-forward` | none | event types |
| `media-rewind` | none | event types |
| `media-track-next` | none | event types |
| `media-track-previous` | none | event types |
| `media-record` | none | event types |
| `lower-volume` | none | event types |
| `raise-volume` | none | event types |
| `mute-volume` | none | event types |
| `left-shift` | all keys as escape codes | event types, all keys as escape codes |
| `left-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `left-alt` | all keys as escape codes | event types, all keys as escape codes |
| `left-super` | all keys as escape codes | event types, all keys as escape codes |
| `left-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `left-meta` | all keys as escape codes | event types, all keys as escape codes |
| `right-shift` | all keys as escape codes | event types, all keys as escape codes |
| `right-ctrl` | all keys as escape codes | event types, all keys as escape codes |
| `right-alt` | all keys as escape codes | event types, all keys as escape codes |
| `right-super` | all keys as escape codes | event types, all keys as escape codes |
| `right-hyper` | all keys as escape codes | event types, all keys as escape codes |
| `right-meta` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level3-shift` | all keys as escape codes | event types, all keys as escape codes |
| `iso-level5-shift` | all keys as escape codes | event types, all keys as escape codes |

## Native actions

Native actions call operations specific to Zellij or Herdr. Their `type` begins with `native.`. When Muxe loads the configuration, it checks the action name and parameters for the terminal multiplexer where you opened the menu. Example: `native.zellij.command:close-focus`.
